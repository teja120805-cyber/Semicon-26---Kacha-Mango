"""Evaluation harness: run the pipeline over a split, compute metrics/
breakdowns, generate plots, and check the model integration gate.

Run modules with `python -m evaluation.<module>` from the project root
(see scripts/evaluate_model.py for the documented entry point).
"""
